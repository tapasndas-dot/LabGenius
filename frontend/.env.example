# Browser-facing base URL. /api uses the Vite development proxy below.
VITE_API_BASE_URL=/api

# Local FastAPI target used only by the Vite development server.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
